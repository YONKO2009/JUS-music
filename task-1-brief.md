### Task 1: Project Scaffolding

**Files:**
- Create: `app/build.gradle.kts` (configured per spec)
- Create: `settings.gradle.kts`
- Create: `build.gradle.kts`
- Modify: `gradle.properties` (added `android.nonTransitiveRClass=true` etc)

**Interfaces:**
- Produces: `com.jusmusic` project directory structure.

- [ ] **Step 1: Create directory structure**
- [ ] **Step 2: Initialize `build.gradle.kts` and `settings.gradle.kts`**
- [ ] **Step 3: Define dependencies as per spec**
- [ ] **Step 4: Commit**
