---
name: font-assets
description: Font assets in app/src/main/res/font are HTML wrapper placeholders and must be replaced with valid .ttf files.
metadata:
  type: reference
---

The current font files for Dela Gothic One and Figtree in `app/src/main/res/font/` are HTML wrappers (not valid binary .ttf files). These must be replaced with actual binary .ttf files from Google Fonts or equivalent sources to function correctly at runtime.
